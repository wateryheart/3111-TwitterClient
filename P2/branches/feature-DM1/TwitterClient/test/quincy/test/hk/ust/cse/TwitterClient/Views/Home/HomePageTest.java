package quincy.test.hk.ust.cse.TwitterClient.Views.Home;

import static org.junit.Assert.*;
import hk.ust.cse.TwitterClient.Views.Home.HomePage;

import org.eclipse.swt.widgets.Display;
import org.eclipse.swt.widgets.Shell;
import org.junit.Test;

public class HomePageTest {
	@Test(timeout=10000)
	  public void testConstructor() throws Throwable {/*
	    Display display = new Display();
	    Shell shell = new Shell(display);
	    HomePage homePage = new HomePage(shell, 10, 10, 10, 10, 10, "abc", "abc", null);
	    assertNotNull(homePage);
	    assertEquals(10, homePage.getBounds().width);
	    shell.dispose();
	    display.dispose();*/
	  }
	}