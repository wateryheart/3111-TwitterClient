package quincy.test.hk.ust.cse.TwitterClient.Views;

import static org.junit.Assert.*;
import hk.ust.cse.TwitterClient.Resources.Resources;
import hk.ust.cse.TwitterClient.Views.TweetView;

import org.eclipse.swt.widgets.Display;
import org.eclipse.swt.widgets.Shell;
import org.junit.Test;

import twitter4j.Status;

public class TweetViewTest {

	@Test(timeout=10000)
	public void testTweetView() throws Throwable {/*
	    Display display = new Display();
	    Shell shell = new Shell(display);
	    Status tweet = null;
	    TweetView view = new TweetView(shell, tweet, 10, true, Resources.HOVER_COLOR, Resources.HOVER_COLOR, Resources.HOVER_COLOR, "abc" ,null);
	    assertNotNull(view);
	    assertEquals(10, view.getBounds().width);
	    shell.dispose();
	    display.dispose();*/
	    
	  }
	}


	
