package quincy.test.hk.ust.cse.TwitterClient;



import static org.junit.Assert.assertNotNull;
import hk.ust.cse.TwitterClient.TwitterClient;




import org.junit.Test;


public class TwitterClientTest {

	@Test(timeout=10000)
	public void testTwitterClient() throws Throwable {
	//    Display display = new Display();
	//    Shell shell = new Shell(display);
	//    TwitterClient client = new TwitterClient(10, 10);
	//    assertNotNull(client);
	  //  assertEquals(10, client.getBounds().width);
	  //  assertEquals(10, client.getBounds().height);
	  //  shell.dispose();
	  //  display.dispose();
		
	}

}
