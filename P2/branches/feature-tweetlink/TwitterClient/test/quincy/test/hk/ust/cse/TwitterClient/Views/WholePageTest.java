package quincy.test.hk.ust.cse.TwitterClient.Views;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertNotNull;
import hk.ust.cse.TwitterClient.Views.WholePage;
import org.junit.Test;
import org.eclipse.swt.widgets.Display;
import org.eclipse.swt.widgets.Shell;
public class WholePageTest {

	@Test(timeout=10000)
	public void testWholePage() throws Throwable {/*
		Display display = new Display();
	    Shell shell = new Shell(display);
		WholePage page = new WholePage(shell,100,100);
		assertNotNull(page);
	    assertEquals(100, page.getBounds().width);
	    assertEquals(100, page.getBounds().height);
	    shell.dispose();
	    display.dispose();*/
		}
	}


