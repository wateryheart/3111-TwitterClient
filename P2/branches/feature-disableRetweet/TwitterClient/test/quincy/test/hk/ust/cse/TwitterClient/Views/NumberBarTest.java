package quincy.test.hk.ust.cse.TwitterClient.Views;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertNotNull;
import hk.ust.cse.TwitterClient.Views.NumberBar;
import hk.ust.cse.TwitterClient.Resources.Resources;
import org.eclipse.swt.widgets.Display;
import org.eclipse.swt.widgets.Shell;
import org.junit.Test;

public class NumberBarTest {
  @Test(timeout=10000)
  public void testConstructor() throws Throwable {/*
    Display display = new Display();
    Shell shell = new Shell(display);
    String titles[] ={"abc","def"};
    long numbers[] ={1,2,3,4,5}; 
    NumberBar noBar = new NumberBar(shell, numbers, titles, 10, 10, 10, Resources.HOVER_COLOR, Resources.HOVER_COLOR, Resources.HOVER_COLOR, Resources.FONT14, Resources.FONT14, true, "abc", null);
    assertNotNull(noBar);
    assertEquals(10, noBar.getBounds().width);
    assertEquals(10, noBar.getBounds().height);
    shell.dispose();
    display.dispose();*/
  }
}
