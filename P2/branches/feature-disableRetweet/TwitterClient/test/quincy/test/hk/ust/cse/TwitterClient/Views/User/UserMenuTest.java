package quincy.test.hk.ust.cse.TwitterClient.Views.User;

import static org.junit.Assert.*;
import hk.ust.cse.TwitterClient.Views.User.UserMenu;

import org.eclipse.swt.widgets.Display;
import org.eclipse.swt.widgets.Shell;
import org.junit.Test;



public class UserMenuTest {
	@Test(timeout=10000)
	  public void testConstructor() throws Throwable { /*
	    Display display = new Display();
	    Shell shell = new Shell(display);
	    
	    UserMenu userMenu = new UserMenu(shell, 10, 10, "abc", null);
	    assertNotNull(userMenu);
	    assertEquals(10, userMenu.getBounds().width);
	    assertEquals(10, userMenu.getBounds().height);
	    
	    shell.dispose();
	    display.dispose();
	    
	    userMenu.setCurrentSelected(10);
	    assertEquals(10, userMenu.getCurrentSelected());*/
	  }
	
	}
