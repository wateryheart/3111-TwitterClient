package quincy.test.hk.ust.cse.TwitterClient.Views;

import static org.junit.Assert.*;
import hk.ust.cse.TwitterClient.Views.NumberBarItem;
import hk.ust.cse.TwitterClient.Resources.Resources;
import org.eclipse.swt.widgets.Display;
import org.eclipse.swt.widgets.Shell;
import org.junit.Test;

public class NumberBarItemTest {

	 @Test(timeout=10000)
	  public void testConstructor() throws Throwable {/*
	    Display display = new Display();
	    Shell shell = new Shell(display);
	    NumberBarItem numberBarItem = new NumberBarItem(shell, 12345, "ABC", 10, 10, Resources.HOVER_COLOR,  Resources.HOVER_COLOR,  Resources.HOVER_COLOR, Resources.FONT11, Resources.FONT11);
	    assertNotNull(numberBarItem);
	    assertEquals(10, numberBarItem.getBounds().width);
	    assertEquals(10, numberBarItem.getBounds().height);
	    assertEquals(12345, numberBarItem.getNumber());
	    assertEquals("ABC", numberBarItem.getTitle());
	    shell.dispose();
	    display.dispose();
	  */}
	}