package quincy.test.hk.ust.cse.TwitterClient.Views.User;

import static org.junit.Assert.*;
import hk.ust.cse.TwitterClient.Resources.Resources;
import hk.ust.cse.TwitterClient.Views.User.UserMenuItem;

import org.eclipse.swt.widgets.Display;
import org.eclipse.swt.widgets.Shell;
import org.junit.Test;



public class UserMenuItemTest {
	 @Test(timeout=10000)
	  public void testConstructor() throws Throwable {/*
	    Display display = new Display();
	    Shell shell = new Shell(display);
	    
	    
	    UserMenuItem userMenuItem = new UserMenuItem(shell,"abc",10, 10, Resources.FONT11, Resources.FONT11);
	    assertNotNull(userMenuItem);
	    assertEquals(10, userMenuItem.getBounds().width);
	    assertEquals(10, userMenuItem.getBounds().height);
	    assertEquals("abc", userMenuItem.getTitle());
	   
	    shell.dispose();
	    display.dispose();*/
	  }

	}

