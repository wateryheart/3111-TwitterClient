package quincy.test.hk.ust.cse.TwitterClient.Views;

import static org.junit.Assert.*;
import hk.ust.cse.TwitterClient.Views.ListView;
import org.eclipse.swt.widgets.Display;
import org.eclipse.swt.widgets.Shell;
import org.junit.Test;



public class ListViewTest {

	@Test
	public void testListViewCompositeInt() throws Throwable {/*
	    Display display = new Display();
	    Shell shell = new Shell(display);
	    ListView view = new ListView(shell, 10);
	    assertNotNull(view);
	    assertEquals(10, view.getBounds().width);

	    shell.dispose();
	    display.dispose();*/

	  }

	@Test
	public void testListViewCompositeStringIntStringObjectStringObject() throws Throwable {/*
	    Display display2 = new Display();
	    Shell shell2 = new Shell(display2);
	    
	    ListView view2 = new ListView(shell2, "abc", 10, "abc", null, "abc", null);
	    assertNotNull(view2);
	    assertEquals(10, view2.getBounds().width);
	    shell2.dispose();
	    display2.dispose();*/
	  }

}