package quincy.test.hk.ust.cse.TwitterClient.Views.User;

import static org.junit.Assert.*;
import hk.ust.cse.TwitterClient.Views.User.UserPage;

import org.eclipse.swt.widgets.Display;
import org.eclipse.swt.widgets.Shell;
import org.junit.Test;

import quincy.test.hk.ust.cse.TwitterClient.Mocks.MockUser2;


public class UserPageTest {
	 @Test(timeout=10000)
	  public void testConstructor() throws Throwable {/*
	    Display display = new Display();
	    Shell shell = new Shell(display);
	    
	    MockUser2 friend = new MockUser2("FakeUser", "FakeScreenName", false, "Fake Description");
	    UserPage userPage = new UserPage(shell, friend,"abc",100, 10, 60, 10, 10, 20, "Lee", null);
	    assertNotNull(userPage);
	    assertEquals("FakeUser", userPage.getUser().getName());
	    assertEquals("FakeScreenName", userPage.getUser().getScreenName());
	    assertEquals("Fake Description", userPage.getUser().getDescription());
	    assertTrue(userPage.getUser().isVerified());
	    
	    shell.dispose();
	    display.dispose();*/
	  }

	}
